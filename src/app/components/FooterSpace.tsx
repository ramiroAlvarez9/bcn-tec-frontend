import Footer from "../idHookPages/footer";

interface Props {}

const FooterSpace = ({}) => {
  return (
    <footer className = "space">
      <Footer />
    </footer>
  );
};

export default FooterSpace;
